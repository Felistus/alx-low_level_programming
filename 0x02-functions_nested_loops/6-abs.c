#include "main.h"
/**
 * int_abs - computes the absolute value of an integer.
 * @n: integer parameter
 * Return: absolute value of given number
 */
int _abs(int)
{
	return (ab * ((ab > 0) - (ab < 0)));
}
