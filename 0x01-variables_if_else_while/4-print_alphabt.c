#include <stdio.h>
/**
 * main - prints all alphabets except q and e
 * Return: 0
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
		if (c != 'q' && c != 'e')
			putchar(p);
	}
	putchar('\n');
	return (0);
}
